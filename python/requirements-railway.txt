# AIFred Trading — Railway Deployment
# ==================================================
# Full ML ensemble enabled (PyTorch CPU-only ~180MB)

# Core data
numpy>=1.25.0
pandas>=2.1.0
scipy>=1.11.0

# Technical analysis (pure Python, no TA-Lib C dep)
pandas-ta>=0.3.14

# ML — full ensemble
scikit-learn>=1.3.0
xgboost>=2.0.0

# PyTorch CPU-only (installed via --index-url in Dockerfile)
torch>=2.0.0
transformers>=4.35.0

# Config & env
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Ethereum signing (required for Hyperliquid order signing)
eth-account>=0.11.0

# Async & networking
aiohttp>=3.9.0
websockets>=12.0
requests>=2.31.0

# Exchange connectors
ccxt>=4.0.0

# NLP (lightweight)
feedparser>=6.0.10

# Reddit sentiment
praw>=7.7.0

# Scheduling
apscheduler>=3.10.4

# Logging
structlog>=23.2.0

# Telegram alerts
python-telegram-bot>=21.0

# Health server
aiohttp>=3.9.0
